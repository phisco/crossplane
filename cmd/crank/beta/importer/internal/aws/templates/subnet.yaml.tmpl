apiVersion: ec2.aws.crossplane.io/v1beta1
kind: Subnet
metadata:
  name: {{ .Object.SubnetId }}
  annotations:
    crossplane.io/external-name: "{{ .Object.SubnetId }}"
spec:
    managementPolicies: ["Observe"]
    forProvider:
        region: "{{ .Region }}"